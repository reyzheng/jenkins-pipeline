File: $file_path
File Context (including line number, if applicable):
- This section contains the file context along with its **starting line number**.
- If this section is **empty**, it means the patch represents an **"Added" operation** (i.e., a new file is introduced).
```
$function
```
Patch Content:
- Apply the following patch to the provided File context (or treat it as a new file if "File Context" is empty).
```
$patch
```
